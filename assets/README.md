# App Icon

This directory should contain the app icon files.

To generate proper icons:
1. Create a 1024x1024 PNG icon
2. Use https://flutter.dev/docs/development/ui/assets-and-images to generate icons
3. Or use the `flutter_launcher_icons` package

For now, the app will use the default Flutter icon.
